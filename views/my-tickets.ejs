<!DOCTYPE html>
<html>
<head>
    <title>Technician Dashboard</title>
    <link rel="stylesheet" href="/styles/style.css">
</head>
<%- include('header') %>
<body>
    <div class="container">
        <% const safeUrl = typeof currentUrl !== 'undefined' ? currentUrl : '/technician'; %>
        <div class="header">
            <h2><%= dashtitle %></h2>
            <div class="header-actions">
                <a href="<%= backUrl %>" class="btn">Back</a>
                <a href="#" onclick="confirmLogout()" class="btn logout-btn">Logout</a>
            </div>
        </div>
        <div class="content">
            <table class="table">
                <tr>
                    <th>ID</th>
                    <th>Title</th>
                    <th>Priority</th>
                    <th>Status</th>
                    <th>Date</th>
                    <th>Actions</th>
                </tr>
            
                <% if (tickets.length === 0) { %>
                    <tr>
                        <td colspan="5">No tickets found</td>
                    </tr>
                <% } else { %>
                    <% tickets.forEach(ticket => { %>
                        <tr>
                            <td><%= ticket.ticket_id %></td>
                            <td><%= ticket.title %></td>
                            <td><%= ticket.priority %></td>
                            <td><%= ticket.status %></td>
                            <td><%= new Date(ticket.created_at).toLocaleString('en-PH', {
                                month: 'short',
                                day: 'numeric',
                                hour: '2-digit',
                                minute: '2-digit'
                            }) %></td>
                            <td>
                                <a href="/tickets/view/<%= ticket.ticket_id %>?backUrl=<%= safeUrl %>" class="btn-dashboard view-btn">
                                    View
                                </a>
                            </td>
                        </tr>
                    <% }) %>
                <% } %>
            </table>
        </div>
    </div>
</body>

<script>
    function confirmLogout() {
        showConfirm("Are you sure you want to logout?", function () {
            window.location.href = "/logout";
        });
    }
</script>